/* Estilos CSS para el roadmap de Solware */

/* Variables de color */
:root {
  --primary-color: #007bff;
  --secondary-color: #14a034;
  --highlight-color: #1d6d65;
  --background-color: #f8f8f863;
  --text-color: #333;
}

/* Estilos globales */
body {
  font-family: 'Montserrat', sans-serif;
  background-color: var(--background-color);
  color: var(--text-color);
  margin: 0;
}

/* Estilos del contenedor principal */
.container {
  display: flex;
  flex-direction: column;
  align-items: center;
  width: 80%;
  margin: 0 auto;
  /* Centra el contenedor en la página */
}

/* Barra de navegación */
nav {
  background-color: #f0f0f0;
  /* Color de fondo de la barra */
  padding: 10px;
  border-bottom: 1px solid #ddd;
  /* Línea divisoria */
}

nav a {
  color: var(--text-color);
  padding: 8px 16px;
  text-decoration: none;
  margin: 0 5px;
  /* Espacio entre enlaces */
  border-radius: 5px;
  /* Bordes redondeados */
  transition: background-color 0.3s ease;
  /* Transición suave */
}

/* Pestaña activa */
nav a.active {
  background-color: var(--primary-color);
  color: white;
}

/* Hover en las pestañas */
nav a:hover {
  background-color: #e0e0e0;
  /* Color de fondo al pasar el ratón */
}

/* Contenido de las pestañas */
.tab-content {
  padding: 20px;
  width: 100%;
  /* Ancho completo del contenedor */
}

/* Ocultar secciones inactivas */
.tab-content:not(.active) {
  display: none;
}

/* Encabezados (h1, h2, h3) */
h1,
h2,
h3 {
  text-align: center;
  /* Alineación de texto al centro para todos los encabezados */
}

h1 {
  /* Título principal del roadmap */
  color: var(--primary-color);
  margin-top: 40px;
  font-size: 3em;
  text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.2);
  /* Sombra sutil para resaltar el título */
}

h2 {
  /* Títulos de las fases del roadmap */
  color: var(--secondary-color);
  padding: 15px;
  margin: 30px 0 10px 0;
  border-radius: 5px;
  /* Bordes ligeramente redondeados */
  box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
  /* Sombra sutil para dar profundidad */
}

/* Listas */
ul {
  list-style: none;
  /* Elimina los puntos de las listas */
  padding-left: 20px;
}

li {
  margin-bottom: 8px;
  /* Espacio entre elementos de lista */
  transition: background-color 0.3s ease, padding 0.3s ease;
  /* Transiciones suaves para efectos hover */
}

li::before {
  /* Añade un punto personalizado a los elementos de lista */
  content: "\2022";
  color: var(--highlight-color);
  display: inline-block;
  width: 1em;
  margin-left: -1em;
  transition: transform 0.3s ease;
}

/* Enlaces */
a {
  color: var(--highlight-color);
  text-decoration: none;
  /* Elimina el subrayado de los enlaces */
  transition: color 0.3s ease;
}

a:hover {
  /* Cambia el color del enlace al pasar el mouse por encima */
  color: #00695C;
  text-decoration: underline;
}

/* Línea de tiempo */
.timeline {
  display: flex;
  flex-direction: column;
  align-items: flex-start;
  position: relative;
  padding-bottom: 20px;
  margin-left: 20px;
}

.timeline::before {
  /* Línea vertical de la línea de tiempo */
  content: '';
  position: absolute;
  top: 0;
  left: 20px;
  width: 2px;
  height: 100%;
  background-color: #2196F3;
}

.timeline-item {
  /* Contenedor de cada fase del roadmap */
  background-color: #fff;
  border-radius: 8px;
  box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
  padding: 25px;
  margin: 30px 0;
  position: relative;
  width: 75%;
  transition: transform 0.3s ease, box-shadow 0.3s ease;
  /* Transiciones suaves para efectos hover */
}

.timeline-item::before {
  /* Círculo en la línea de tiempo */
  content: '';
  position: absolute;
  top: 50%;
  left: -12px;
  transform: translateY(-50%);
  width: 24px;
  height: 24px;
  border-radius: 50%;
  background-color: #2196F3;
  z-index: 1;
  transition: transform 0.3s ease, background-color 0.3s ease;
}

.timeline-item h3 {
  /* Título de la fase */
  color: var(--highlight-color);
  margin-top: 0;
  display: flex;
  align-items: center;
}

.timeline-item h3 i {
  /* Icono de la fase */
  margin-right: 10px;
  transition: transform 0.3s ease;
}

/* Efectos hover para los elementos de la línea de tiempo */
.timeline-item:hover,
li:hover {
  transform: translateY(-5px);
  /* Desplazamiento suave hacia arriba */
  box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
  /* Sombra más intensa */
}

.timeline-item:hover {
  background-color: #f7f7f7;
  /* Cambio de color de fondo */
}

.timeline-item:hover::before {
  transform: scale(1.2) translateY(-50%);
  /* Agrandar y elevar el círculo */
  background-color: #00BCD4;
  /* Cambiar color de fondo del círculo */
}

.timeline-item h3:hover i {
  transform: rotate(360deg);
  /* Rotar el icono */
}

.timeline-item:hover h3 {
  color: #00695C;
  /* Cambiar color del texto del título */
}

li:hover {
  background-color: #e0f7fa;
  /* Cambiar color de fondo de los elementos de lista */
  padding: 18px;
  border-left: 4px solid #00BCD4;
  /* Agregar borde izquierdo */
}

li:hover::before {
  transform: scale(1.2);
  /* Agrandar el icono de la lista */
  background-color: #00BCD4;
  /* Cambiar color de fondo del icono de la lista */
}

/* Efecto de onda (ripple) */
.ripple {
  position: relative;
  overflow: hidden;
}

.ripple:hover {
  overflow: visible;
}

.ripple:hover::after {
  content: '';
  position: absolute;
  top: 50%;
  left: 50%;
  transform: translate(-50%, -50%);
  width: 0;
  height: 0;
  border-radius: 50%;
  background-color: #00BCD4;
  animation: ripple 0.5s ease-out;
}

@keyframes ripple {
  0% {
    width: 0;
    height: 0;
  }

  100% {
    width: 200px;
    height: 200px;
    opacity: 0;
  }
}